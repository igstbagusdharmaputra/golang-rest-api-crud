# Golang Rest API CRUD with Gin Framework
## How to Use
- **Edit .env.example to .env File**
  ```sh
  cd config
  cp .env.example .env
  ```
- **Up Service with docker-compose**
  ```sh
  docker-compose up -d --build
  ```
- **Down Service with docker-compose**
  ```sh
  docker-compose down
  ```